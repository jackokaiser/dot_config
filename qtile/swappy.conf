# Config for swappy, used by the screenshot bindings in config.py.
# save_dir must match SHOT_DIR there.
[Default]
save_dir=$HOME/Pictures/screenshots
save_filename_format=%Y-%m-%d-%H%M%S.png
show_panel=true
